# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
use_bioc: true
bioc_required: true
r:
  - bioc-release
  - bioc-devel    # Errors as of 20180105. Something off with devtools.
  - oldrel
  - release
  - devel    # Errors as of 20180105, 20180316 (devel at this point was 3.4.4)
# this package builds just fine on 3.4.4

sudo: true
cache: packages

notifications:
  email:
    recipients:
      - gabriel.odom@med.miami.edu
